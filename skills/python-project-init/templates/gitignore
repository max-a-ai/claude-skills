# UV / Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Type checker / lint caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# IDE
.idea/
.vscode/

# data-management (machine-dependent, never committed)
data/
checkpoints/
outputs/

# NOTE: .docs/ is deliberately NOT ignored -- it is the record of
# the work (progress.md, figures, promoted runs).

# OS
.DS_Store
