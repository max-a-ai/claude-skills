# Todo — {{PROJECT_NAME}}

Open tasks. Ordered roughly by priority — pick what's ripe.

---

## Now

- [ ] (first feature / v1 scope goes here)

## Next

- [ ] (stuff to do after v1 ships)

## Backlog

- [ ] (ideas that might or might not happen)

---

## Guardrails

- Keep dependencies minimal. Every new dep needs a rationale line in
  `HANDOFF.md`'s Confirmed Decisions table.
- New modules: append a node to `.vault-{{PROJECT_NAME}}/{{PROJECT_NAME}}.canvas`
  so the dashboard stays current.
- End of each work session: append a dated entry to `progress.md`.
