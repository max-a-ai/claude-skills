# Progress — {{PROJECT_NAME}}

## Gantt

```mermaid
gantt
    title {{PROJECT_NAME}}
    dateFormat  YYYY-MM-DD
    axisFormat  %m-%d
    section Scaffold
    UV + pyproject + ruff/mypy cfg  :done, s1, {{TODAY}}, 1d
    Vault (canvas + progress + todo) :done, s2, {{TODAY}}, 1d
    section Core
    (first feature goes here)       :active, c1, {{TODAY}}, 3d
```

## Log

### {{TODAY}} — Initial scaffold
- **Instruction:** scaffold new Python project `{{PROJECT_NAME}}`.
- **Decisions:**
  - Python {{PYTHON_VERSION}}, UV, hatchling, ruff (79 cols), mypy strict.
  - src/ layout with `src/{{MODULE_NAME}}/`.
  - `.vault-{{PROJECT_NAME}}/` for progress log, todo, and Obsidian canvas.
- **Steps:**
  1. `uv init --python {{PYTHON_VERSION}} --package --name {{PROJECT_NAME}}`.
  2. Overwrote `pyproject.toml` with hatchling + ruff + mypy strict configs.
  3. Created `.vault-{{PROJECT_NAME}}/` with canvas, progress, todo.
  4. Created `instructions.md`, `HANDOFF.md`, `README.md`, `.gitignore`.
- **Result:** scaffold passes `ruff check`, `ruff format --check`, `mypy --strict`.
